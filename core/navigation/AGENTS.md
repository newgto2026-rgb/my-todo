# :core:navigation Module Delta

## Scope
- App-wide navigation contracts shared across app and feature modules.

## Module Rules
- Keep only stable contract types (for example `AppFeatureEntry`).
- Do not place feature-specific route implementations here.
- Keep this module free from business logic and UI rendering logic.
